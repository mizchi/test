{
  "name": "@mizchi/testio",
  "version": "0.0.4",
  "module": "dist/testio.js",
  "main": "dist/testio.cjs",
  "types": "types/index.d.ts",
  "scripts": {
    "dev": "vite",
    "build": "LIB=1 vite build && yarn tsc",
    "prepublishOnly": "yarn build",
    "test": "tsc -p . --noEmit --emitDeclarationOnly false && node -r esbuild-register test.ts"
  },
  "devDependencies": {
    "@types/node": "^16.10.1",
    "esbuild": "^0.13.2",
    "esbuild-register": "^3.0.0",
    "typescript": "^4.3.2",
    "vite": "^2.4.2"
  },
  "files": [
    "src",
    "types",
    "dist"
  ]
}
