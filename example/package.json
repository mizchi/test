{
  "private": true,
  "scripts": {
    "test": "NODE_ENV=test node -r esbuild-register index.ts",
    "test-only": "NODE_ENV=test node -r esbuild-register only.ts"
  },
  "devDependencies": {
    "@mizchi/test": "^0.1.0",
    "esbuild": "^0.13.3",
    "esbuild-register": "^3.0.0"
  }
}
